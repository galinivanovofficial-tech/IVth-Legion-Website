{
  "name": "ivth-legion",
  "version": "1.0.0",
  "private": true,
  "description": "IVth Legion — membership website with Stripe subscriptions and automated Telegram access. Zero runtime dependencies.",
  "type": "module",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node --disable-warning=ExperimentalWarning src/index.js",
    "dev": "node --disable-warning=ExperimentalWarning --watch src/index.js",
    "check": "node --check src/index.js && node --check src/config.js && node --check src/db.js && node --check src/auth.js && node --check src/stripe.js && node --check src/telegram.js && node --check src/jobs.js"
  }
}
